using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Abilities : MonoBehaviour
{
    /*

    void Update()
    {
        if(Input.GetKey(KeyCode.LeftArrow))
        {
            shoot ghost in direction the player is facing
        }
        if (Input.GetKey(KeyCode.DownArrow))
        {
            place a camping ghost at the player's current position
        }
        if (Input.GetKey(KeyCode.RightArrow))
        {
            shoot a mapping ghost that will turn randomly when they see a path to turn to
        }
        if (Input.GetKey(KeyCode.UpArrow))
        {
            Hunt();
        }
    }
    private void Hunt()
    {
        ghosts turn solid;
        enemies turn hollow;
        enemies leave breadcrumbs?
        change music to Light theme;

        if (any ghost collides with enemy)
        {
            enemy dies;
        }

        if (enemy is outside of vision bubble)
        {
            render enemy;
        }
    }
    */
}
